# runntime

Placeholder release. Real package coming soon.

The `./core`, `./zoo` and `./zoo/transformers` entry points in this repo are
API-surface stubs: the public types are real, every function throws. They exist
so `apps/docs` builds and typechecks against the shape the library will have.
They are not published — `files` ships `index.js` only.
